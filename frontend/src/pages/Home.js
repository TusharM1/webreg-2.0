import React from "react";
//Defualt page before login
//Handles functionality when user is moved from a student or admin dashboard from lacking a token
const Home = () => {
	return (
		<span>Home</span>
	);
};

export default Home;