import React from 'react';

const Dashboard = (token) => {
    // console.log("Here Dashboard");

    return (
        <main>
            <h1>Dashboard</h1>
        </main>
    );
};

export default Dashboard;